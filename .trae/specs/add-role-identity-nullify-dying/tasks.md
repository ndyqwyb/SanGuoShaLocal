# Tasks
- [x] 任务 1：引入身份与座次基础设施
  - [x] 在 `player.py` 增加身份字段与枚举（至少主公/反贼），并提供 `is_ally/is_enemy` 判定接口（多人扩展预留）。
  - [x] 在 `game.py` 增加座次与逆时针遍历工具（从任意起点遍历存活角色一圈）。
  - [x] 1v1 初始化时自动赋予身份：玩家主公、AI 反贼（UI 默认不展示）。

- [x] 任务 2：重构无懈可击结算（全场轮询 + 无懈对无懈）
  - [x] 替换现有 `_is_trick_countered`：实现“从起点逆时针全场轮询”的通用无懈链函数（支持多轮反转）。
  - [x] 非延时锦囊：起点为锦囊使用者；提示语按 spec 更新为“即将生效/失效…是否无懈”。
  - [x] 延时锦囊：起点为延时锦囊目标；提示语按 spec 更新。
  - [x] AI 无懈策略接入身份判定（队友不无懈、敌人倾向无懈；并保留 1v1 下可运行）。

- [x] 任务 3：重构五谷丰登（多人轮转 + 每次选牌前可无懈跳过）
  - [x] 按存活人数亮 N 张牌作为候选池。
  - [x] 以使用者为起点，逆时针依次轮到每名存活角色选牌；每次选牌前执行一次“全场无懈（跳过本次选牌）”轮询。
  - [x] 实现“最后一人仅剩 1 张且未被无懈时自动获得”特例；其他情况保持选择流程。
  - [x] 选完后剩余牌入弃牌堆；补齐 AI 在该流程中的无懈决策。

- [x] 任务 4：重构濒死阶段（全场求桃 + 暂停非救援结算）
  - [x] 引入“濒死流程入口”：当 HP≤0 时进入濒死并启动全场求桃。
  - [x] 实现从“玩家（人类）”开始逆时针询问全场救援：桃/急救等；救援成功则退出濒死，失败则死亡。
  - [x] 将“非救援技能/事件”（如 after_damage 的遗计/反馈/刚烈等）改为在濒死结束后再结算（需有可验证的延后机制）。
  - [x] AI 救援策略按身份判定：敌人不救，队友/自己救。

- [x] 任务 5：UI/提示语与可用性交付
  - [x] 更新无懈相关 prompt 文案为 spec 指定格式（包含使用者/锦囊/目标/生效状态）。
  - [x] 更新濒死求桃 prompt 文案为“全场求桃”语义（并确保 GUI 的 `yesno/skill_card_select` 可正常操作）。
  - [x] 1v1 模式不展示身份标签（但保留后续开启展示的开关位）。

- [x] 任务 6：测试与回归
  - [x] 单元测试：无懈链（至少 2 人场景，覆盖“无懈对无懈”反转）；延时锦囊无懈链起点为目标。
  - [x] 单元测试：五谷丰登轮转与“无懈跳过”。
  - [x] 单元测试：濒死全场求桃（至少 2 人场景），并验证 after_damage 类技能在濒死结束后才触发。
  - [x] 运行 `pytest -q` 全量回归通过。

# Task Dependencies
- 任务 2/3/4 依赖 任务 1（身份与座次工具）。
- 任务 5 依赖 任务 2/3/4 的 prompt 与流程落地。
- 任务 6 依赖 任务 1~5 完成后统一执行。
